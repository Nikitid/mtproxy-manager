# mtproto-manager
VibeCode cooked this. I’m just the waiter

Features
install/remove MTProxy
restart/start proxy
update MTProxy without changing user secret
change secret
change TLS domain
show active clients
built-in stats
Requirements
Debian/Ubuntu
root access
